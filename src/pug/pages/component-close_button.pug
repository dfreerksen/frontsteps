extends ../layouts/2col.pug

block append config
  - var pageTitle = "Close button";
  - var pageSubtitle = "A close button for dismissing content like modals and alerts";
  - var skipMenu = true;

block content
  .card
    .card-body
      h4 Example
      p Provide an option to dismiss or close a component with <code>.btn-close</code>. Default styling is limited, but highly customizable. Modify the Sass variables to replace the default <code>background-image</code>. <strong>Be sure to include text for screen readers</strong>, as we've done with <code>aria-label</code>.

      .bd-example
        button.btn-close(type='button' aria-label='Close')

      pre
        code.language-html
          | &lt;button type='button' class="btn-close" aria-label='Close'&gt;&lt;/button&gt;

      h4 Disabled state
      p Disabled close buttons change their <code>opacity</code>. We've also applied <code>pointer-events: none</code> and <code>user-select: none</code> to preventing hover and active states from triggering.

      .bd-example
        button.btn-close(type='button' disabled='' aria-label='Close')

      pre
        code.language-html
          | &lt;button type='button' class="btn-close" disabled aria-label='Close'&gt;&lt;/button&gt;

      h4 Dark variant
      p Add <code>data-bs-theme="dark"</code> to the <code>.btn-close</code>, or to its parent element, to invert the close button. This uses the <code>filter</code> property to invert the <code>background-image</code> without overriding its value.

      .bd-example
        div(data-bs-theme='dark')
          button.btn-close(type='button' aria-label='Close')
          button.btn-close(type='button' disabled='' aria-label='Close')

      pre
        code.language-html
          | &lt;div data-bs-theme="dark"&gt;
          |   &lt;button type='button' class="btn-close" aria-label='Close'&gt;&lt;/button&gt;
          |   &lt;button type='button' class="btn-close" disabled aria-label='Close'&gt;&lt;/button&gt;
          | &lt;/div&gt;
